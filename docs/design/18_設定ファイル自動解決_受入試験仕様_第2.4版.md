# 設定ファイル自動解決 受入試験仕様 第2.4版

| ID | Provider | 試験内容 | 期待結果 |
|---|---|---|---|
| CFG-A-001 | Labonity | 正常な `LibLocal.xml` とUNCパス | サーバーフォルダとDB設定XMLを解決する。 |
| CFG-A-002 | Labonity | `p_サーバーフォルダパス` が空欄 | `CFG-LAB-002` で起動停止。 |
| CFG-A-003 | Labonity | XMLにDTDを含める | DTDを処理せず構成エラー。 |
| CFG-A-004 | Labonity | DB設定XML不在 | `CFG-LAB-003`。別Providerへ切り替えない。 |
| CFG-A-005 | Labonity | NaDat名変更 | 再読込後の接続検証が成功してから切替。 |
| CFG-B-001 | LegacyLiberty | CP932の `LIBLocal.Ini` | 正常に読込む。 |
| CFG-B-002 | LegacyLiberty | コメントされたサーバーパスと有効行 | コメントを無視し有効行を採用。 |
| CFG-B-003 | LegacyLiberty | `D:\Dat2\` | Agentホストのローカルパスとして解決。 |
| CFG-B-004 | LegacyLiberty | UNCパス | サービスアカウントで到達確認。 |
| CFG-B-005 | LegacyLiberty | `LIBCTRL.ini` 不在 | `CFG-LIB-003`。 |
| CFG-B-006 | LegacyLiberty | 全角 `ＳＱＬユーザーＩＤ` | NFKCで正規化して取得。 |
| CFG-B-007 | LegacyLiberty | パスワードに `;` と `=` を含む | 最初の `=` だけで分割し、値を欠落させない。 |
| CFG-B-008 | LegacyLiberty | 同一キーが2回 | 最後の有効値を採用し警告。 |
| CFG-B-009 | LegacyLiberty | LMASTER空欄 | `CFG-LIB-006`。既定値へフォールバックしない。 |
| CFG-B-010 | LegacyLiberty | `工場コード使用=0`、行コード空欄 | 初期工場コードを使用。 |
| CFG-B-011 | LegacyLiberty | `工場コード使用=1`、行コード空欄 | 行を同期せずエラー記録。 |
| CFG-C-001 | 共通 | Provider不一致のファイルを指定 | 形式不正で停止し、自動判定しない。 |
| CFG-C-002 | 共通 | SQLパスワードを含む例外 | ログ、画面、テレメトリに値が出ない。 |
| CFG-C-003 | 共通 | 二次設定変更中に一時的な不完全ファイル | debounce後に検証し、旧設定を維持。 |
| CFG-C-004 | 共通 | SQL Server名変更 | 全ヘルスチェック後、管理者承認が必要。 |
| UPD-A-001 | 共通 | 正常な署名済み更新 | 設定を保持して更新し、ヘルスチェック成功。 |
| UPD-A-002 | 共通 | SHA-256不一致 | 更新せず `UPD-002`。 |
| UPD-A-003 | 共通 | 更新後DB接続失敗 | 自動ロールバックし `UPD-003`。 |
